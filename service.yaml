apiVersion: v1
kind: Service
metadata:
  name: aceest-service
spec:
  type: NodePort
  selector:
    app: aceest   # for blue-green switch
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30007